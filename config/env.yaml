APP_ID: org.digitalgreen.farmer.chat
LANGUAGE: English (Kenya)
LANGUAGE_CODE: en
USER_NAME: Test Farmer
SHORT_NAME: TF
WAIT_TIMEOUT: 10000
# Secrets (PHONE_NUMBER, NEW_PHONE_NUMBER, OTP_CODE) live in the gitignored
# config/env.local.yaml — copy config/env.local.example.yaml and fill them in.
# Tests tagged `# requires: PHONE_NUMBER,OTP_CODE` skip when they're unset.
